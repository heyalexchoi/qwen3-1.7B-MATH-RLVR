# Core
# torch 2.6.0+ required — TRL 1.0.0 imports FSDPModule which was added in PyTorch 2.6.
# runpod/pytorch:2.4.0 image ships 2.4.1; upgrade after pod creation:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
torch>=2.6.0
transformers>=4.51.0
datasets>=2.14.0
accelerate>=0.25.0
peft>=0.7.0

# Training (TRL for SFT + GRPO)
trl>=1.0.0
wandb>=0.16.0

# vLLM — primary inference backend (continuous batching, fast batch eval)
vllm>=0.4.0

# Flash attention — optional, used by HF fallback path for long-sequence speed
# Requires compilation: pip install flash-attn --no-build-isolation
# flash-attn>=2.5.0

# API clients
anthropic>=0.40.0

# Evaluation
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
math-verify[antlr4_13_2]  # ANTLR4/SymPy math evaluator (primary answer checker)

# Utilities
jsonlines>=4.0.0
pyyaml>=6.0
